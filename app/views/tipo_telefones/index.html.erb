<p id="notice"><%= notice %></p>

<h1>Tipo Telefones</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_telefones.each do |tipo_telefone| %>
      <tr>
        <td><%= tipo_telefone.nome %></td>
        <td><%= link_to 'Show', tipo_telefone %></td>
        <td><%= link_to 'Edit', edit_tipo_telefone_path(tipo_telefone) %></td>
        <td><%= link_to 'Destroy', tipo_telefone, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo Telefone', new_tipo_telefone_path %>
